import 'package:flutter/material.dart';

import '../../components/models/user_profile_data.dart';

class FriendsPage extends StatelessWidget {
  const FriendsPage(this.userProfileData, {super.key});

  final UserProfileData userProfileData;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('cxzcscsdfdfs'),
      ),
    );
  }
}
